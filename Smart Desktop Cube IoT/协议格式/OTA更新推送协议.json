{
  "code": int,                  // 200=有新版本，204=已是最新
  "type": string,               // 固定：ota_update
  "msg": string,                // 说明信息（如 "新版本可用" / "已是最新版本"）
  "timestamp": int,             // 服务器时间戳
  "url": string,                // 固件下载地址（HTTP/HTTPS）
  "version": string,            // 新固件版本号（如 "v1.2.0"）
  "md5": string                 // 固件文件MD5校验值（32位小写，可选）
}

code说明：
200 = 有新版本可用（设备网页端显示确认更新按钮）
204 = 当前已是最新版本（无需更新）
401 = Token无效
403 = 设备未授权

注意：设备收到 code=200 后不会自动更新，
而是等待用户通过网页端点击"确认更新"后才执行OTA下载。
